node_modules/
out/
dist/
release/
*.log
.DS_Store
.env
.env.local
tsconfig.*.tsbuildinfo

# Claude provider (kept locally, not tracked)

# Recorded demos: the GIF is committed, the MP4 is uploaded to a
# GitHub issue comment instead of living in git history.
docs/demo.mp4
